# chess-tui

also hexapawn

```
Chess GUI and AI

Usage: chess-ai [OPTIONS]

Options:
  -s, --serial    Communicate with a Serial Port (arduino), requires hexapawn
  -x, --hexapawn  Play Hexapawn instead of Chess
  -h, --help      Print help
  -V, --version   Print version
```